# Copy to .env (or export vars) before running in production
# If using a JSON key file, set GOOGLE_APPLICATION_CREDENTIALS to its path
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

# Or embed the JSON content directly (safer for containers/CI)
# FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account",...}'

# API server port
# PORT=8080

